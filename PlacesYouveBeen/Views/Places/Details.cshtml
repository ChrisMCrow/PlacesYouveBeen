<!DOCTYPE html>
<html>
  <head>
    <title>PlacesYouveBeen</title>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
  </head>
  <body>
    <div class="container">
      <h1 class="jumbotron">@Model.GetCity()</h1>
      <h3>@Model.GetCity(), @Model.GetCountry()</p>
      <p>I was there for @Model.GetDuration()</p>
      <p>Here's what I did:</p>
      <p>@Model.GetActivities()</p>
      <a href="/places"><button class="btn btn-info">Back to Places</button></a>
    </div>
  </body>
</html>
